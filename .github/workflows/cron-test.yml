name: Cron Test (Diagnostic)

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  cron-check:
    runs-on: ubuntu-latest

    steps:
      - name: Print time
        run: |
          echo "✅ CRON TRIGGERED"
          date
